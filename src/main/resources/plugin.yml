name: DupePlugin
version: '${project.version}'
main: esp.storage.dupecommand.DupePlugin
api-version: 1.18
authors: [ StorageESP, ImFound ]
description: /dupe command

commands:
  dupe:
    description: Dupes the item in your hand
    usage: /<command>